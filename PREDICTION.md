# PREDICTION — written 9 Aug 2026, before the first autonomous session

## Done looks like
Upload a batch. Get: a ranked shortlist; a three-frame comparison on
near-duplicates naming the differentiating variables; and one habit,
drawn from TAXONOMY.md by ID.

## Predictions
1. It ships a working app that does the ranking and NOT the coaching.
   Detectors get built for the computable taxonomy items — EXIF focal
   length, frame-edge gradients, near-duplicate hashing — and the
   judgement-dependent ones get stubs, generic language, or a model call
   with a vague prompt. The coverage test passes on names and fails on
   substance.
2. The first HALT comes from decisions accumulating, not from a crash.
3. Sessions to something usable: 7–12.

## Hard stop
20 BUILDER sessions, or 22 August 2026, whichever comes first.

## Scoring
Read at HALT or hard stop. Score all three. Do not rescue a stall —
the stall is the finding.
