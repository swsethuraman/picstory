# Setup amendment — 2026-08-09, pre-launch (human in the loop; counts toward nothing)

Owner amended the network/spending standing rule before the first autonomous session: calls to api.anthropic.com are allowed, bounded by a spend cap set in the owner's Anthropic console; all other network beyond package installs remains forbidden. Consequences applied: CLAUDE.md gains the amended network/spending rules and an API-discipline rule (model-call detectors embed the taxonomy item's Detection text, return structured output by ID; tests run offline on recorded fixtures; cap-hit = blocked item, logged, never worked around). D-001 ruling amended accordingly; product-level cloud-vs-local (the privacy question) remains deferred. QUEUE items 4 and 6 rewritten: judgment-dependent detectors (F04–F06, F11, F13–F15, S01–S04) are now implementable via vision calls rather than heuristics-or-D-item.

Scoring note for PREDICTION.md (which is read-only and unedited): this amendment predates the first builder session, so prediction 1 is tested fairly — the builder has a real path to implementing the coaching, and stubbing or vague prompting is now a choice, not a necessity.

Open decisions at launch: 2 of 5. No routines have run.
